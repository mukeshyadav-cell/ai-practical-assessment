---
description: Core project context, tech stack, and scope for the AEM Support Ticket Management System
alwaysApply: true
---

# Project: AEM Support Ticket Management System

## Purpose
A full-stack Support Ticket Management System built on AEM as a Cloud Service (AEMaaCS),
demonstrating responsible AI-assisted development across the full SDLC.

## Tech Stack (STRICT)
- AEM as a Cloud Service SDK (AEMaaCS), archetype 57
- Java 21 (code MUST remain Java 17 compatible — avoid Java 21-only preview features)
- Maven multi-module: all, core, ui.apps, ui.apps.structure, ui.config, ui.content, ui.frontend, dispatcher, it.tests
- Content Fragments for data persistence (Ticket, Comment)
- Users seeded via AEM UserManager (JCR)
- Sling Servlets for ALL data access (REST at /bin/api/v1/*) — NO GraphQL
- HTL (Sightly) + vanilla JS for UI
- Repository Pattern (interface + CF adapter, must be swappable to a DB adapter later)

## Project Coordinates (ACTUAL — use exactly)
- Base package: com.mysite.core
- App id: assessment
- App path: /apps/assessment
- Config path: /conf/assessment
- Content path: /content/assessment
- DAM path: /content/dam/assessment

## Scope — Mandatory Core ONLY
Entities:
- User (seeded): id, name, email
- Ticket: id, title, description, priority, status, assignedTo, createdBy, createdAt, updatedAt
- Comment: id, ticketId, message, createdBy, createdAt

Functionality:
- Create ticket, list tickets, view ticket detail, update fields, reassign,
  add comment, keyword search, status filter
- Enforce the ticket state machine (see 02-state-machine rule)

## Out of Scope (do NOT build unless explicitly asked)
- User CRUD/authentication beyond AEM's built-in login
- Sprints, dashboards, project management, notifications, attachments
- GraphQL, external databases (design for it, do NOT implement yet)

## When Unsure
- If you don't know an exact AEM/Sling API, SAY SO — do not invent methods
- Prefer official AEM/Sling APIs and cite the class you are using
